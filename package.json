{
  "name": "long-exposure-camera",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "On-device long-exposure frame stacking for iPhone Safari.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "test:browser": "node tests/browser.check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
